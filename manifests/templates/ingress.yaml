---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boottr                  # Ingress name
  labels:
    app: boottr
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "boottr.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: boottr               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
